import { SquadmatesStatusPipe } from './squadmates-status.pipe';

describe('SquadmatesStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SquadmatesStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
